* Chapter 2
** Recursion (2.7)
#+begin_src lisp
  (defun our-member (obj 1st)
    (if (null 1st)
        nil
        (if (eql (car 1st) obj)
            1st
            (our-member obj (cdr 1st))))) 
#+end_src

#+RESULTS:
: OUR-MEMBER

*** JS
#+begin_src js :tangle ./book.js
  let ourMember = obj => list =>
      ((list === [])?
       (nill) :
       ((list[0] === obj)?
        (list[0]):
        (ourMember(obj)(list.slice(1)))));

  let cdr = list => list.slice(1);
#+end_src
